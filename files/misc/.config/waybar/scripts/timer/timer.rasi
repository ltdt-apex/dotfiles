@import "~/.config/rofi/config.rasi"

/* ---- Entry ---- */
entry {
  width: 27%;
  placeholder: "select Timer, or type to insert new one";
}

/* ---- Window ---- */
window {
    width:	30%;
}

/* ---- Message ---- */
textbox {
    background-color:            @selected;
    text-color:                  @background;
}